/* =====================================================
   GEOPOTAMO · Ejemplo de configuración local
   =====================================================
   Copia este archivo como  js/env.js  y completa con
   tus credenciales reales de Supabase.

   Dónde encontrarlas:
     supabase.com → tu proyecto → Settings → API
     · Project URL   → SUPABASE_URL
     · anon / public → SUPABASE_ANON_KEY

   ⚠  js/env.js está en .gitignore — nunca lo subas.
      Este archivo (env.example.js) SÍ se puede subir.
===================================================== */

window.GEOPOTAMO_CONFIG = {
  SUPABASE_URL:      "https://TU_PROJECT_ID.supabase.co",
  SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
};
